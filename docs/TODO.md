# TODO

更新日期：2026-05-10

## 剧本与机制

- 重写开拍后 9 幕母版：当前代码版更像 AI 指令素材库，前端可直接播放的成稿行不够多。下一版应改成“逐句可播放母版”，让 `剧中内容`、`内心`、`片场压力`、`演员反应`、`老赵救场`都直接落成 VN 行。
- 接入 Claude 重写稿：收到新稿后，需要把 9 幕文字转成代码里的结构，并保留 XML actor 标签作为演员碰撞入口。
- 增加 `playableLines` 或同等结构：不要只依赖 `referenceText/defaultSetReaction` 让 AI 扩写，默认无 AI 的 demo 也要有足够体量。
- 开拍前生成演员“入戏偏差卡”：根据选角、说服词和入组心态，生成每个演员进入角色后的表演理解，用于后续每幕碰撞。
- 优化工具干预后的连贯性：喊卡、改词、加鸡腿、导演示范不只改当前句，还要稳定影响剩余幕的演员反应和剧情走向。

## 前端体验

- 继续调 VN 对话框视觉：目前已多轮修正宽度和高度，还需要在完整流程里统一检查镜头、动作、内心、台词、演员反应几类框的比例。
- 做一次完整视觉回归：从封面、开场、捞人、说服、开拍、结算完整跑一遍，重点看人物立绘位置、框线、文字遮挡、按钮遮挡和移动端适配。
- 优化开拍阶段信息密度：片场状态、导演工具、已拍事实和主文本区域需要继续平衡，避免右侧栏和主体画面互相抢注意力。
- 历史记录窗口继续打磨：需要确认入口、展示样式、滚动体验是否自然。

## AI 与部署

- 用真实 AI 环境测试 `/api/draft`、`/api/intervene`、`/api/revise-act` 输出质量，检查是否仍会泄露“雷点/炸点/系统提示/XML tag”等内部词。
- 优化 AI 延迟策略：初始 9 幕预生成、工具干预只改当前幕剩余内容、后续幕进入前再修订，这套机制还需要在真实接口下验证等待时间。
- Vercel 环境变量配置：部署时只在 Vercel 配置 `AI_API_KEY`，不要提交 `.env.local`。
- Vercel 协作权限确认：如果同伴要从 GitHub 触发部署，需要确认 Vercel 项目归属、团队成员或仓库公开策略。

## 素材与音效

- 清理未使用素材：确认旧像素图、临时场景图、重复立绘是否仍被代码引用，未引用的再移除。
- 人物立绘最终校准：日常立绘和戏服立绘已经接入，但还需要逐幕检查比例、裁切、透明边缘和不同演员之间的统一性。
- 音效混音：现在已接入 BGM、音效和语气词，但还需要按场景校准音量、触发频率和是否打断阅读。

## 工程

- 为剧本数据加校验：检查每幕行数、每行 type、speaker、sourceBeatId、XML tag 是否合法。
- 增加基础测试或脚本：至少验证 build、剧本数据解析、mock draft 输出、内部词过滤。
- GitHub/Vercel README 更新：补充本地运行、环境变量、部署方式和素材目录说明。
